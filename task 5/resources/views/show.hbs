{{>header}}
{{>navbar}}
<section class="container my-5">
    {{#if user}}
    <h4>{{user.name}}</h4>
    <span>{{user.age}}</span>
    <p>{{user.status}}</p>
    {{else}}
    <div class="text-center">
        <p>Invalid data</p>
        <a href="/" class="btn btn-primary">Home</a>
    </div>
    {{/if}}
</section>
{{>footer}}