{{>header}}
{{>navbar}}
<section class="container my-5">
    {{#if post}}
    <h4>{{post.title}}</h4>
    <span>{{post.date}}</span>
    <p>{{post.body}}</p>
    {{else}}
    <div class="text-center">
        <p>Invalid data</p>
        <a href="/" class="btn btn-primary">Home</a>
    </div>
    {{/if}}
</section>
{{>footer}}